<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./Partials/_head') %>
</head>
<body>
    <h1>CABIN</h1>

    <% if(items) { %>
        <% items.forEach (e => { %>
        
        <%= e.name %>
        <% })} %>
</body>
</html>